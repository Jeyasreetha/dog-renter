<div class="container my-5">
  <div class="d-flex flex-column bd-highlight mb-2">
    <div class="d-flex align-items-end flex-column bd-highlight mb-3" style="height: 80px;">
      <div class="p-2 bd-highlight"><p><%= link_to "Edit your profile", edit_user_registration_path, class: "btn btn-primary"%></p></div>
    </div>
    <div class="p-2 bd-highlight text-center">
      <% if @user.image? %>
      <img class="avatar-large" alt="avatar-large" src="<%= @user.image %>">
      <% else %>
      <img class="avatar-large" alt="avatar-large" src="https://upload.wikimedia.org/wikipedia/commons/thumb/7/7e/Circle-icons-profile.svg/1024px-Circle-icons-profile.svg.png">
      <% end %>
    </div>

    <div class="p-2 bd-highlight">
      <h2 class="card-description text-center">Hello, I'm <%=@user.name%></h2>
    </div>
    <div class="p-2 bd-highlight">
      <p class="card-description text-center">Joined in <%= @user.created_at.strftime("%Y") %> | From <%= @user.location%></p>
    </div>
  </div>
  <div class="container my-5 bg-white p-3" >
    <% if @user.description? %>
    <%= @user.description %>
    <% else %>
    <p>Add a description by editing your profile. It should include a little bit about yourself and your experience with dogs - so users know you better.</p>
    <% end %>
  </div>
  <div class="container my-5 bg-white p-3">
     <div class="d-flex align-items-end flex-column bd-highlight" style="height: 10px;">
        <div class="p-2 bd-highlight"><p> <%= link_to "Add your dog", new_dog_path, class: "btn btn-outline-secondary"  %></p></div>
    </div>
    <h3>My dogs</h3>

    <ul class="p-2 bd-highlight">
      <% @dogs.each do |dog| %>
      <li style="list-style-type:none"><i class="fas fa-paw"></i> <%= link_to dog.name, dog_path(dog) %></li>
      <% end %>
    </ul>
  </div>

  <div class="container my-5 bg-white p-3" >
    <h3>Dogs you rented</h3>
    <ul class="p-2 bd-highlight">
      <% @rentings.each do |renting| %>
      <li style="list-style-type:none"><i class="fas fa-paw"></i> <%= link_to dog.name, dog_path(dog) %></li>
      <% end %>
    </ul>
  </div>
</div>
